{% comment %}
  VARENAL Testimonials / Social Proof section
{% endcomment %}

<section class="testimonials full-width section-padding">
  <div class="section-heading">
    <h2>{{ section.settings.title | default: 'What Our Customers Say' }}</h2>
    <p>{{ section.settings.subtitle }}</p>
  </div>

  <div class="testimonials__grid">
    {% for block in section.blocks %}
      <div class="testimonials__card" {{ block.shopify_attributes }}>
        <div class="testimonials__stars">
          {% assign stars = block.settings.rating | default: 5 %}
          {% for i in (1..stars) %}
            <svg width="18" height="18" viewBox="0 0 24 24" fill="var(--color-secondary)" stroke="none"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
          {% endfor %}
        </div>
        <p class="testimonials__quote">{{ block.settings.quote }}</p>
        <div class="testimonials__author">
          <div class="testimonials__avatar">
            {{ block.settings.name | slice: 0 }}
          </div>
          <div>
            <strong class="testimonials__name">{{ block.settings.name }}</strong>
            {% if block.settings.verified %}
              <span class="testimonials__verified">✓ Compra Verificada</span>
            {% endif %}
          </div>
        </div>
      </div>
    {% endfor %}
  </div>
</section>

{% stylesheet %}
  .testimonials {
    background: linear-gradient(180deg, #f8fafb 0%, #ffffff 100%);
  }

  .testimonials__grid {
    max-width: min(var(--page-width), calc(100% - var(--page-margin) * 2));
    margin: 0 auto;
    padding: 0 var(--page-margin);
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
  }

  .testimonials__card {
    background: #ffffff;
    border: 1px solid rgba(0, 0, 0, 0.06);
    border-radius: 16px;
    padding: 2rem;
    transition: all 0.3s ease;
  }

  .testimonials__card:hover {
    box-shadow: 0 12px 30px rgba(0, 0, 0, 0.06);
    transform: translateY(-3px);
  }

  .testimonials__stars {
    display: flex;
    gap: 2px;
    margin-bottom: 1rem;
  }

  .testimonials__quote {
    font-size: 0.95rem;
    color: #444;
    line-height: 1.7;
    margin-bottom: 1.5rem;
    font-style: italic;
  }

  .testimonials__author {
    display: flex;
    align-items: center;
    gap: 0.8rem;
  }

  .testimonials__avatar {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background: var(--gradient-brand);
    color: #fff;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 700;
    font-size: 1rem;
    text-transform: uppercase;
  }

  .testimonials__name {
    font-size: 0.9rem;
    display: block;
  }

  .testimonials__verified {
    font-size: 0.75rem;
    color: var(--color-primary);
    font-weight: 500;
  }

  @media (max-width: 768px) {
    .testimonials__grid {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }
  }

  @media (min-width: 769px) and (max-width: 1024px) {
    .testimonials__grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }
{% endstylesheet %}

{% schema %}
{
  "name": "Testimonials",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Title",
      "default": "Lo Que Dicen Nuestros Clientes"
    },
    {
      "type": "textarea",
      "id": "subtitle",
      "label": "Subtitle",
      "default": "Resultados reales de personas reales que confían en VARENAL para su confort diario."
    }
  ],
  "blocks": [
    {
      "type": "testimonial",
      "name": "Testimonial",
      "settings": [
        {
          "type": "range",
          "id": "rating",
          "min": 1,
          "max": 5,
          "step": 1,
          "label": "Star rating",
          "default": 5
        },
        {
          "type": "textarea",
          "id": "quote",
          "label": "Quote",
          "default": "¡Este masajeador de tobillo es increíble! El calor terapéutico realmente ayuda después de un largo día de pie."
        },
        {
          "type": "text",
          "id": "name",
          "label": "Name",
          "default": "Sarah M."
        },
        {
          "type": "checkbox",
          "id": "verified",
          "label": "Verified buyer",
          "default": true
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Testimonials",
      "blocks": [
        {
          "type": "testimonial",
          "settings": {
            "rating": 5,
            "quote": "Llevo meses con dolor de tobillo por estar de pie en el trabajo. Esta almohadilla con vibración es exactamente lo que necesitaba — ¡los 3 niveles de calor son perfectos y me encanta el diseño inalámbrico!",
            "name": "Sara M.",
            "verified": true
          }
        },
        {
          "type": "testimonial",
          "settings": {
            "rating": 5,
            "quote": "Súper fácil de usar con la pantalla táctil. Me encanta que se apague automáticamente — puedo relajarme sin preocupaciones. Se ajusta perfecto a ambos tobillos. Excelente calidad por el precio.",
            "name": "Jaime R.",
            "verified": true
          }
        },
        {
          "type": "testimonial",
          "settings": {
            "rating": 5,
            "quote": "Lo compré como regalo para mi mamá que camina mucho. ¡Le encanta! El forro de felpa es suave y la batería dura varias sesiones. Totalmente recomendado.",
            "name": "María L.",
            "verified": true
          }
        }
      ]
    }
  ],
  "disabled_on": {
    "groups": ["header", "footer"]
  }
}
{% endschema %}
